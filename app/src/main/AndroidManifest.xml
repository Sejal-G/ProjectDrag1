<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vasu.projectdrag">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".Login" />
        <activity android:name=".SSOReg" />
        <activity android:name=".Home" />
        <activity android:name=".DonorReg" />
        <activity android:name=".SSO" />
        <activity android:name=".Donor" />
        <activity android:name=".DProfileEdit" />
        <activity android:name=".SProfileEdit" />
        <activity android:name=".SProfile" />
        <activity android:name=".DEvent"></activity>
    </application>

</manifest>